{
  "name": "weave",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first relationship intelligence engine for macOS.",
  "main": "dist/main/main/index.js",
  "type": "commonjs",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "dev:main": "tsc -p tsconfig.main.json --watch --preserveWatchOutput",
    "dev:preload": "tsc -p tsconfig.preload.json --watch --preserveWatchOutput",
    "dev:electron": "concurrently -k \"npm:dev\" \"npm:dev:main\" \"npm:dev:preload\" \"wait-on http://127.0.0.1:5173 dist/main/main/index.js dist/preload/preload/index.js && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .\"",
    "build": "tsc -p tsconfig.json && vite build && npm run build:main && npm run build:preload",
    "build:main": "tsc -p tsconfig.main.json",
    "build:preload": "tsc -p tsconfig.preload.json",
    "build:ocr": "swiftc native/ocr/WeaveOCR.swift -framework AppKit -framework Vision -framework CoreGraphics -o native/ocr/weave-ocr",
    "build:contacts": "swiftc src/main/scripts/fetch_apple_contacts.swift -framework Contacts -framework Foundation -o src/main/scripts/fetch_apple_contacts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.main.json && tsc --noEmit -p tsconfig.preload.json"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.2",
    "clsx": "^2.1.1",
    "drizzle-orm": "^0.30.10",
    "googleapis": "^134.0.0",
    "lucide-react": "^0.468.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "sql.js": "^1.10.3",
    "zod": "^3.22.4"

  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/react": "^18.2.73",
    "@types/react-dom": "^18.2.22",
    "@types/sql.js": "^1.4.9",
    "@vitejs/plugin-react": "^4.2.1",
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "electron": "^29.1.6",
    "typescript": "^5.4.3",
    "vite": "^5.2.6",
    "vitest": "^1.4.0",
    "wait-on": "^7.2.0"
  }
}
